# Admin Portal Credentials (Optional - Override with Environment Variables)
VITE_ADMIN_USERNAME=your_username
VITE_ADMIN_PASSWORD=your_secure_password

# IMPORTANT: Change these default values before deploying to production!
# Store this file securely and never commit it to version control.
